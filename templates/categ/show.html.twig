{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ category.type }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ category.status }}</td>
            </tr>
            <tr>
                <th>Features</th>
                <td>{{ category.features }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categ_index') }}">back to list</a>

    <a href="{{ path('app_categ_edit', {'id': category.id}) }}">edit</a>

    {{ include('categ/_delete_form.html.twig') }}
{% endblock %}
