<form method="post" action="{{ path('app_stations_delete', {'id': station.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ station.id) }}">
    <button class="btn4">Supprimer</button>
</form>
