{% extends 'base.html.twig' %}

{% block title %}Station{% endblock %}

{% block body %}
    <h1>Station</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ station.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ station.name }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ station.location }}</td>
            </tr>
            <tr>
                <th>OpenHour</th>
                <td>{{ station.openHour }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn3"href="{{ path('app_stations_index') }}">Retour</a>

    <a class="btn2" href="{{ path('app_stations_edit', {'id': station.id}) }}">Modifier</a>

    {{ include('stations/_delete_form.html.twig') }}
{% endblock %}
